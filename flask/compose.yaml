version: '3'

services:
  flask:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - /home/harbhlahritik/flask/uploads:/app/uploads
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1

